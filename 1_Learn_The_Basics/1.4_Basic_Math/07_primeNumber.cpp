/*
#include <math.h>
bool isPrime(int n)
{
    int sq = sqrt(n);

    if(n==1)
        return false;

    for(int i=2; i<=sq; i++){
        if(n%i==0)
            return false;
    }

    return true;
}

*/