/*
int removeDuplicates(vector<int> &arr, int n) {
    for(int i=0; i< arr.size() -1 ; i++){
        if(arr[i+1] == arr[i]){
            arr.erase(arr.begin() + i);
            i--;
        }
    }

    return arr.size();
}
*/